 language: generic
 sudo: required
 services:
    - docker

before_install:
    - docker build -t brandonian/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true brandonian/docker-react npm run test